import 'package:flutter/material.dart';

class AppColors {
  // Premium Porsche-inspired Color Palette
  static const Color primary = Color(0xFF1E3F20);       // Rich Porsche Forest/Racing Green
  static const Color primaryLight = Color(0xFF2E5B32);  // Lighter green for highlights and gradients
  static const Color primaryDark = Color(0xFF112513);   // Very deep shadow green

  static const Color secondary = Color(0xFF8B9287);     // Porsche GT Silver / Metallic Sage
  static const Color accent = Color(0xFFD4AF37);        // Porsche Emblem Gold (Metallic)
  static const Color accentRed = Color(0xFFE31B23);     // Porsche Emblem Red (Alerts/Active states)
  static const Color silver = Color(0xFFD4D8D5);        // Sleek silver-grey for borders/subtle texts

  // Background & Surface
  static const Color background = Color(0xFFF4F6F4);    // Elegant, soft grayish-green background
  static const Color surface = Color(0xFFFFFFFF);       // Crisp white surface for cards
  
  // Typography Colors
  static const Color textPrimary = Color(0xFF14241B);   // Extremely dark green-black for primary text
  static const Color textSecondary = Color(0xFF5E6D62); // Slate grey-green for readable secondary text
  static const Color textLight = Color(0xFFFFFFFF);     // White text for dark surfaces

  // Semantic
  static const Color border = Color(0xFFE2E6E2);        // Sophisticated subtle borders
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFFFA000);
  static const Color error = Color(0xFFC62828);
}
